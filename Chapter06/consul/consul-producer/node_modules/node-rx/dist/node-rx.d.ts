import { Store } from './store';
import { Actions, EffectsSubscription } from './effects';
export declare class NodeRx<T> {
    private static initialReducerFactory(reducer);
    private static initialStateFactory(initialState, reducer);
    private static storeFactory<T>(dispatcher, reducer, state$);
    store: Store<T>;
    effectSubscription: EffectsSubscription;
    actions: Actions;
    private state;
    private dispatcher;
    private reducer;
    constructor(initialReducer: any, initialState: any, effectsInstance?: any[]);
}
